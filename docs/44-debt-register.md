# 44 — Technical Debt Register
Status: Living · Rule: debt is *chosen* and recorded at decision time with a repayment trigger, or it is a defect.

| ID | Debt | Chosen because | Interest we pay | Repayment trigger |
|---|---|---|---|---|
| TD-01 | DataFusion for Q1 SQL instead of tile-native engine | quarter scope | extra copy sheet→Arrow; ACL enforcement wrapped, not planned-in | Arrow layer lands (H2) or query p95 breach |
| TD-02 | ADRs 001–026 written post-hoc | rewrite speed | rationales may hide rejected-alternative context | live-ADR rule from now; backfill alternatives when touched |
| TD-03 | Spec budgets are targets, not measurements | design preceded rig | credibility risk; possible re-design | MEASUREMENTS.md Q1 W13 |
| ~~TD-04~~ **PAID (session 29)** | ~~XLSX write values-only in Q1~~ → `usk_xlsx::write` emits values, formulas with cached results, and number formats at both levels — the reader's whole modelled surface — with a named `WriteReport` for everything it cannot carry (D-121/D-122/D-123). Round-trip fidelity over the 20-file corpus: **100.0%** of modelled cells, and all 20 written files open in real Excel (COM-verified). Original entry: | scope | demo asymmetry (read ≫ write) | **CLOSED session 29** by exactly what the trigger named: write + corpus round-trip + a published number (W-XLSX-WRITE, MEASUREMENTS.md) |
| TD-05 | No gRPC/Arrow in H1 | desktop-first trim | bulk integrations wait; REST/NDJSON only | first data-platform design partner |
| TD-06 | Auth = bearer token in Q1 skeleton | scope | not production-safe; must not leak into Q3 | server plane alpha (Q3) replaces wholesale |
| TD-07 | Charts/pivots absent at GA-alpha boundary | scope discipline | pilot objections expected | H2 top of queue; pre-briefed to partners |
| TD-08 | Single relay process (no HA) through Q3 | ops simplicity | availability risk in pilots | GA operational-readiness gate |
| ~~TD-09~~ **PAID (session 9)** | ~~Promotion granularity is the whole 16,384-cell tile~~ → promotion is now per contested **cell**; amplification 16,384× → 1×, measured under W-TILE-10M. A-001 restored under collaboration (123.6 MB vs 400 MB). Original entry | ADR-005 as designed; the cheap summary is per tile, so the promoted unit is too | Measured: one contested cell costs ~1 MB of per-cell metadata; 0.1% scattered contention drives 8.4 → 74.5 B/cell and puts 10M cells at ~745 MB vs a 400 MB budget (A-002 FAILED, MEASUREMENTS.md) | **CLOSED session 9.** Chose per-cell stamps over sub-tile blocks: blocks only move the amplification constant, cells remove it. Row 10's gate is satisfied. |
| TD-10 | Promotion detects *multi-writer cells*, not true concurrency | v0.1 `Op` carries `{id, lamport, payload}` — docs/10's causal `deps` delta is not implemented yet, so causally-ordered cross-actor writes are indistinguishable from concurrent ones | Over-promotion: sequential hand-offs (A edits, then B edits the same cell later) pay full per-cell metadata for a conflict that never existed | `Op` gains `deps` (Row 10 sync). Re-run `tools/tile-bench`; expect the A-002 numbers to improve but **not** enough to close TD-09 on its own |
| TD-12 | `Decimal` is a 128-bit scaled integer, not IEEE 754-2008 decimal128 (ADR-035) | Full IEEE decimal128 is thousands of lines plus a conformance suite, in a `no_std` kernel with a 5-crate budget; the available crates offer *fewer* digits (~28) than either | No bit-level interchange with other decimal128 implementations; no ±Inf/NaN; no quantum. In exchange: 38 digits vs decimal128's 34, and exact integer arithmetic | XLSX round-trip or an external system requiring true decimal128 bit-encoding. Localised behind `Value::Decimal` + the canonical encoder |
| ~~TD-13~~ **PAID (session 12)** | ~~`compat_final_adjust`'s `1e-15` threshold is documentation-derived (D-041)~~ → measured: **8 ULP of the larger operand**, strict, positional for `+`/`-` and unconditional inside `SUM`/`AVERAGE` (D-041 as amended, docs/50 findings 1/2/2b) | The Excel COM capture harness (ADR-024, A-007) did not exist yet, and docs/32 warns the documentation lies | The interest was larger than priced: the rule was not merely unvalidated, it was **wrong**, and it was **one rule where there are two** — the unconditional aggregate mechanism was entirely unrepresented | **CLOSED session 12** by the first oracle corpus, exactly as the trigger said. The debt entry did its job: it named the vector to capture first, and that vector refuted the decision |
| TD-32 | `compat_parse_15` is unimplemented: the engine does not truncate numeric literals to 15 significant digits at parse time, does not reject `>=1E308`, does not flush `1E-308` to zero, and does not normalise `-0` away (D-081) | Found by the first oracle capture (docs/50 finding 3), *after* the lexer shipped. It is destructive and belongs in the lexer, so it cannot be layered on as a display concern later — and it changes the meaning of authored text under `Profile::Compat` | Any `compat` workbook containing a literal of more than 15 significant digits, or at the extremes of the double range, diverges at parse time — before evaluation, so no amount of correct arithmetic recovers it | Implement with the conformance runner watching, so the W-ORACLE number moves visibly. The vectors already exist (`grids/91-literal-parser.psd1`) |
| TD-14 | Lookup is exact-match only; approximate match returns `#N/A` (D-044) | Excel's approximate match is correct only on a sorted key, and v0.1 cannot verify sortedness. A binary search over unsorted data returns a confidently wrong number rather than failing | Imported workbooks using `VLOOKUP(...,TRUE)` — the Excel *default* — get `#N/A` instead of a value. A real compat gap, not a cosmetic one | Row 8 identity intervals give ranges a sortedness contract; implement then, with oracle vectors |
| TD-15 | `^` and `SQRT` use in-crate series/Newton rather than libm (D-046) | `no_std` kernel (DP-A3) and DP-A2's bit-identical-everywhere requirement; platform math libraries disagree in the last bits and drift between versions | Fractional exponents are accurate to ~1e-12, not correctly rounded. Integer exponents are exact, so the common case is unaffected | Oracle corpus (ADR-024) shows a conformance miss attributable to rounding |
| TD-16 | Range-to-scalar collapse takes the top-left cell, not Excel's implicit intersection (`@`) | Implicit intersection needs the *calling cell's* row and column, which arrives with the dependency graph | A formula like `=A1:A10+1` in a scalar context gives Excel's answer only when the caller sits on the first row | Row 7, when the dep graph supplies the evaluating cell's position |
| TD-17 | Recalculation is single-threaded; levels are computed but not executed in parallel | rayon sits behind the PAL `Compute` trait (DP-A3), which is unbuilt | A-003's "on 8 cores" is unvalidated. Currently harmless — the single-threaded path already beats the budget — but a wide model has parallel width this leaves on the floor | **Only when a real workload breaches the 200 ms budget single-threaded** (owner directive 2026-08-07, D-054). Do not build PAL `Compute` + the rayon bench speculatively; until then A-003 stays “confirmed (single-threaded path)”. **Re-measured session 21: 105.8 ms, 53% of budget — trigger not live.** The same run reports `max parallel width ~ 1.0` for W-CHAIN-100K, so a chain has no width to exploit even if it were |
| ~~TD-18~~ **PAID (session 9)** | ~~`Engine::build` regroups the whole sheet~~ → `Engine::observe` routes structural/formula ops to a regroup and value ops to the incremental path (D-059). Register drift found by the v0.1 audit: closed in session 9, never marked here | Row 7's claim is about graph size and recalc cost, not edit-time maintenance | Editing a formula requires a full rebuild (699 ms at 100k formulas), so formula edits are not yet incremental — only *value* edits are | **CLOSED session 9** as the Row 9 exit criterion required (D-054) |
| TD-19 | Graph build is **857 ms** for 100k formulas (median of 5, range 801–918, session 21; the 699 ms on record was a single unreplicated sample), dominated by parsing | No parse cache; every formula string is parsed on build | Extrapolates to ~7 s for 1M formulas — **the superlinearity that made that wrong is fixed (TD-66, session 25)**: the quadratic was `extent_of`, not the parsing, and a 1,000,000-row corpus now builds in **5.36 s** against the 218 s it took before. The extrapolation is roughly right again, and what remains here really is the parse cost. Not a budget breach today (docs/31's 1.5 s cold open covers skeleton+viewport, not a full parse) but it is on the path to one | Row 11 snapshots — persist parsed formulas rather than re-parsing on open |
| TD-20 | The range index is band-bucketed, not the R-tree docs/13 specifies | A band bucket gives the right access shape for a fraction of the code | **The measured problem behind this row is fixed (session 27); the row's stated claim is not, and stays open.** What session 26 measured was not the bucketing — it was that `stab` narrowed to a band and then scanned **every** rectangle a candidate group owned, discarding the narrowing it had just done. That was a **defect, not debt**: nobody chose it, and docs/44's own rule says debt is chosen at decision time or it is a defect. It had been sitting inside this row. A band now holds the rectangles crossing it, each tagged with its group; same answer, work proportional to the query. At 500,000 rows the gapped corpus went **11.33 → 4.50–4.86 µs per formula** and is now indistinguishable from the dense one, where it had been 1.7× worse | Genuinely quiet now: the structure is still a band bucket rather than an R-tree, and no measurement asks for the difference. Revisit if a profile shows band scanning dominating again, or if Row 8 moves rectangles into identity space and the question changes shape |
| ~~TD-21~~ **PAID (session 9)** | ~~Two addressing models coexist in `usk-calc`~~ → `Sheet`/`Cell`/`CellRef` deleted; `Engine` addresses cells by `(RowId, ColId)` and references rebind from stored identity bindings on every rebuild (D-058). Register drift found by the v0.1 audit | Converting the dependency graph to identity intervals is larger than Row 8's scope and would put an unproven rewrite under the A-003 benchmark | Two ways to name a cell in one crate. Harmless while `Sheet` is only the graph's substrate; a correctness hazard the moment both are load-bearing | **CLOSED session 9**; the cost it created is measured as TD-23 |
| ~~TD-22~~ **PAID (session 10)** | ~~Formula-vs-value cell LWW depends on full-replay order, not stamps (D-056)~~ → the registry is a stamped LWW register seeded by the replay pre-pass (D-063); order-independence proven over all 120 permutations of a mixed history, replay hashes unchanged | Both `State` constructors replayed in canonical total order, so last-applied-wins was correct with zero storage | An incremental merge would have resolved formula-vs-value conflicts wrongly | **CLOSED session 10**, before the first incremental-apply path existed — exactly as the trigger required |
| ~~TD-24~~ **LARGELY PAID (session 10)** | ~~`Session::refresh` re-folds the whole log per batch~~ → the fold is now taken **when state is read**, not when the log grows (D-071). W-SYNC-RELAY at 50 replicas: **120 min → 6.8 min, 17.6×**, with every protocol figure bit-identical | A summary tile keeps no per-cell stamps to compare against (ADR-005's memory argument) and promotion must be decided before the first write lands; stamping every cell is the 480 MB regression TD-09 removed | *Residual*: `State` is still not incrementally appliable, so a read after N appends is O(N). Reads are ~50× rarer than appends under sync, which is where the 17.6× came from — but a read-heavy workload (a UI repainting per op) would still pay | **Residual trigger CORRECTED at Row 11** (D-076): the snapshot does *not* give the fold a checkpoint, because a v0.1 snapshot body is the compacted op set and `verify` replays it — W-OPEN-1M's 2.10 s cold open is that replay. The residual closes only when the body becomes a **materialised state image** (docs/16's tile image), which is real work rather than wiring. Still not the resident writer index, which costs memory on sparse workbooks |
| ~~TD-25~~ **PAID (session 12)** | ~~Unknown op tags are a decode error, not preserved-opaque (D-066)~~ → `u32` length framing in the snapshot body, the container's tail stream and the wire (the wire already had it), plus `Payload::Opaque` (D-080). DP-A5 forward preservation is now implemented and tested end to end | The canonical encoding carries no per-op length, so an unknown payload cannot be skipped — the decoder cannot know where it ends | DP-A5 forward preservation ("unknown op types are preserved, causally ordered, hashed opaque, retransmitted") was unimplementable on the wire until the encoding was framed | **CLOSED session 12**, before the first wire-version bump exactly as the trigger required. The prefix sits *outside* the hash, so no canonical op encoding moved; the replay hashes changed only because the corpus gained an opaque op per docs/29 |
| TD-26 | Anti-entropy answers "what does the peer lack" from the never-drop queue and the relay's retained log, not from a Merkle comparison | Exact and O(unacked) for the two-replica and relay-fanout cases Row 10 proves; docs/15's tile-Merkle needs a snapshot Merkle that does not exist yet | Localising divergence inside a *large* shared history costs a retained-log scan instead of docs/15's O(log n) | Row 11 lands the snapshot Merkle; wire it here then |
| ~~TD-30~~ **PAID (session 12)** | ~~A single snapshot is not a fallback (D-075)~~ → docs/16 §Retention now states the policy (keep the last 3 snapshots + all ops since the **oldest** retained one) and `Container::compact` enforces it, with the floor restricted to a snapshot that verifies and no pruning below two verified snapshots (D-079) | Nobody stated a retention policy; the snapshot table's primary key is the watermark, which quietly permits exactly one row per watermark and no history | W-OPEN-1M measured it: corrupting the snapshot's final page lost 1,002,000 ops with **zero quarantined bytes** — nothing damaged, nothing left. The salvage path behaved exactly as specified; the policy above it did not exist | **CLOSED session 12** by architect ruling — both candidate fixes taken, because either alone still had a hole. Proven by `corrupting_every_snapshot_but_the_oldest_still_recovers_every_op`, `corrupting_all_snapshots_rebuilds_from_the_full_op_tail`, and `a_first_compaction_prunes_no_ops_and_survives_losing_its_only_snapshot` |
| TD-31 | Three retained snapshots hold three copies of the covered history | A v0.1 snapshot body **is** the compacted op set (D-069), so retention multiplies it. docs/16's designed body is a Merkle-shared tile image where extra snapshots cost O(dirty) and this vanishes | Container size grows with the retention depth instead of staying flat — W-OPEN-1M's 108 MB is the one-snapshot figure. Recoverability was the right side of that trade (D-079), but the bill is real | The snapshot body becomes a materialised tile image — the same change TD-24's residual and TD-29 both wait on, so all three are repaid together |
| TD-29 | `snapshots.body` is stored uncompressed; docs/26 specifies "zstd tile+object image" | zstd is a fourth dependency stack on top of the nineteen crates `rusqlite` already cost (D-073), and compression changes no property Row 11 proves — the state hash is verified over the *decoded* ops either way | A 1M-cell snapshot body is 90 MB where zstd would plausibly make it a fraction of that; W-OPEN-1M's 108 MB container is mostly this | When container size becomes a user-visible complaint, or when the body becomes a tile image (TD-24's residual) and the two changes can be made together |
| ~~TD-28~~ **PAID (session 11)** | ~~Row 11's SQLite container adapter cannot be built on this host: `rusqlite` builds only via `bundled`, which compiles C, and the pinned toolchain ships a link-only gcc driver with no `cc1` (D-068)~~ | Not written blind — DP-C4 forbids stacking an unverified layer on an unbuildable one | Row 11's container half could not be written, so the storage seam was split and half of Row 11 stalled | **CLOSED session 11** by D-073's in-workspace MinGW-w64 msvcrt toolchain. **This row is also this register's worked example of stale status** (D-078): the blocker was cleared in session 11 and the row was not updated for seven sessions, so it kept describing a condition that no longer existed — and the vacancy is what let the ID be reused (see TD-48) |
| ~~TD-48~~ **RETIRED (session 20) — the claim was false when written** | ~~"CI has never run and the repo has never been pushed"~~ — filed in session 18 *under TD-28's number*, which is the defect this row exists to record. It was **untrue at the moment it was written**: CI had already run four times and the supply-chain job went green on 2026-08-08 (run `31263838366`) | Nothing was chosen. A second, unrelated condition was written into an ID that a paid-but-unmarked row had left looking vacant | Compounding: the false form reached a release audit and was *strengthened* rather than caught, because re-verification checked the numbers the document quoted and not the status it asserted. `19013e2` corrected PROGRESS.md, but V0.1.md had already copied the stale form | **CLOSED.** Rows 12–15 committed (`74c9e7c`); `main` + `v0.1.0` pushed, CI green on every job (`31299292276`). Given its own ID in session 20 so that **one ID means one thing** — the conflation was itself the hazard, and TD-47 below shows the same failure recurring in a different row. Register IDs are append-only: a paid row is struck through, never reoccupied |
| TD-27 | Transport is length-prefixed frames over loopback TCP, not the WebSocket docs/15 names (D-065) | ~20 crates against a DP-S2 ceiling of 40 that stands at 10, plus a second hashing stack DP-B9 forbids | A browser cannot reach the relay directly, so the web shell needs the swap before it can sync | The web shell (Q2), or the dependency budget being raised deliberately. Localised to `frame.rs` + `main.rs` |
| ~~TD-23~~ **PAID (session 26)** | ~~Calc results are an identity-keyed `BTreeMap`; full recalc regressed 53.0 → 92.6 ms on W-CHAIN-100K~~ → **slot-indexed by derived position** (D-118). A dense `Vec` of values, one slot per formula cell, reached through a per-column sorted list of formula rows. Keying by position is safe because `regroup` clears the results and rebuilds the binder together — anything that could move a position has already emptied the map. **W-CHAIN-100K full recalc 112–138 → 41–47 ms on the same host, which is below the 53.0 the regression started from.** Original entry: | Identity keys were what TD-21 left behind when ordinal addressing was deleted; nothing had measured what they cost until W-RECALC-PROFILE | Was: 100k tree inserts and lookups per full recalc. Measured before fixing: the map cost **1.2 µs per formula** — and **nothing** per read, which corrected the guess this work began from. Reads miss the map, and the miss was never the expensive part | Paid |
| TD-11 | `State::replay_sorted` trusts its caller to supply canonical order | The A-001 harness cannot exist otherwise: 10M cells is ~1.2 GB of ops for ~84 MB of state, so the log cannot be materialised | A caller feeding unsorted ops gets silently wrong LWW in summary tiles (a `debug_assert` catches it in dev builds only) | Row 11/12, when snapshot recovery and import become real callers — replace the precondition with a checked, streaming merge |

| TD-37 | The import sandbox has **no syscall filter**, so docs/24's *"seccomp/platform equivalent, no network"* is only half enforced. Address-space isolation, a committed-memory cap, a process-count cap of 1, kill-on-close, a wall-clock cap, an output cap and a fresh process per document are all real (Windows job object); **"no network" is *structural*** — `ehkatra-parse` links no networking code and the DP-S2 budget plus the host-isolation grep are what keep it that way | Windows has no seccomp equivalent reachable without a driver or an AppContainer profile, both of which need installation or elevation — barred by DP-S5 and CLAUDE.md's workspace boundary | A parser compromised by a malicious document could open a socket, and nothing at the OS level would stop it. The blast radius is still limited to the child process, which holds no workbook and no credentials, but exfiltration of *the document being parsed* is not prevented | An AppContainer profile once the app has an installer (it can be applied without elevation from a packaged app), or the POSIX port where `seccomp`/`pledge` are available. **Before any server-side import fleet**, where the parser sees other tenants' documents |
| ~~TD-38~~ **PAID (session 14)** | ~~XLSX read is not implemented~~ → `usk-zip` (ZIP + DEFLATE), `usk-xml` (pull reader) and `usk-xlsx` (values, formulas, number formats), all `no_std`, all zero-dependency, all through the same sandbox. 20-file corpus and per-file fidelity report in MEASUREMENTS.md | It needed a ZIP reader with DEFLATE and an XML parser under a budget with 11 slots left | Row 12 was half done | **CLOSED session 14.** Workspace dependency closure is unchanged at 29/40 — the three decoders cost nothing (D-090) |
| TD-39 | XLSX read models **values, formulas and number formats only**. Charts, drawings, pivot tables, themes, tables, conditional formatting, data validation, defined names, merged cells and column widths are read as *nothing* | BOOTSTRAP row 12 asks for "values+formulas"; the rest is out of v0.1 scope, and XLSX *write* is explicitly excluded so there is no round-trip obligation yet | A file using any of them imports as a grid of values. Measured, not guessed: `part_coverage` puts `14-unmodelled-parts.xlsx` at 50% and names every dropped part | Q2, when XLSX write makes fidelity a round-trip property rather than a read-coverage one. The report already names what is missing, so the work is enumerable |
| TD-40 | Date cells are read as **serial numbers or text**, never as dates. `t="d"` (ISO-8601) is kept as text, and a serial with a date number format stays a number | D-043 deferred `Value::Date` to the format layer, which does not exist. Inventing a conversion here would put a date model in the importer rather than in the engine | A date column imports as numbers with a format code attached. Correct, and not what a user expects to see | The date layer (TD-33's territory). The number format is already carried on the cell, so the information needed is present |
| TD-41 | The ZIP reader ignores **ZIP64**. A container with more than 65,535 entries, or any part over 4 GB, is misread or refused | The 32-bit central directory covers every XLSX the corpus and the format's own limits produce; ZIP64 is a second header layout and a second end-of-directory record | A very large workbook — a million-row sheet with many parts — could exceed it. Excel writes ZIP64 above those thresholds, so this is a real ceiling and not a theoretical one | A corpus file that needs it, or the first user report. The caps refuse rather than misread, so the failure mode is a named error |
| TD-42 | **The MCP `query` (SQL) tool is not shipped** (D-095). BOOTSTRAP row 14 lists it; DataFusion is not added | DataFusion's closure is well over a hundred crates against a DP-S2 ceiling of 40 standing at 29. TD-01 already prices the Arrow layer as H2 work, and this is the same dependency by another route | An agent cannot aggregate or filter server-side. `describe_sheet` answers the shape questions and `read_range` returns rows, so the agent does the aggregation itself over a bounded read — more round trips, and a ceiling on the size of question it can ask | The Arrow layer (TD-01's trigger). Repaid where the machinery already lands, not before |
| TD-43 | The MCP server exposes **one workbook per process** and no document lifecycle — no open, no save, no import. `describe_workbook` reports a single sheet because v0.1's `State` is one grid | BOOTSTRAP row 14 asks for the tool loop, and the container (`ehkatra-store`) and importers (`ehkatra-io`) are separate binaries. Wiring them together is the app, which does not exist | An agent can only work on a workbook the host process seeded. The desktop-local loopback shape docs/21 describes — an agent driving the user's *open* workbook — needs the app | The desktop shell (Q2). The seam is `Server::session()`, which already hands out the live `Session` |
| TD-44 | W-CHAIN-100K regressed **92.6 → 114.0 ms** full recalc (+23%) and **0.328 → 0.618 ms** incremental (+88%) when the compat cancellation rule landed | D-041's positional rule runs `compat_final_adjust` on every top-level `+`/`-` formula, and the chain workload is 100,000 formulas of exactly that shape — the worst case for the rule, which is why it is the number quoted | 23% of full-recalc time. Both budgets still pass with margin (114.0 vs 200 ms; 0.618 vs 8 ms), so this is cost, not breach. It buys 79 oracle cases (W-ORACLE) and is not removable without giving them up | A W-* workload approaching its budget. The cheap half is already taken: `eval_top` re-evaluated its operands at first and cost 145.2 ms; evaluating them once recovered 31 ms. What is left is the rule. **Re-measured session 21: 105.8 ms full and 0.436 ms incremental — 53% and 5.5% of budget, so the trigger is not live** |
| TD-45 | W-OPEN-1M cold open is **7.86 s** and the container **307 MB** for a 1M-cell workbook under docs/16's retention policy | Three retained snapshots, each carrying the compacted op set (D-069), plus every op since the oldest — the price of TD-30's guarantee that one corrupt snapshot loses nothing, which the same run measures as `lost_data = false` | Opening a large workbook is slow and the file is 2.8× larger than with one snapshot. docs/31's 1.5 s budget is for skeleton + viewport rather than a full replay, so this is still neither a pass nor a breach — but it is further from one | The **tile-image snapshot body**: extra snapshots become O(dirty) and opening stops being a replay. Closes TD-31 and TD-24's residual at the same time — one change, three entries |
| TD-46 | **The tile image is built but is not the snapshot body** (D-101). `usk_state::image` round-trips a `State` to the same hash, chunks per tile for structural sharing, and is fuzz-clean — and `snapshots.body` is still the compacted op set | Adopting an image and applying a tail loses the **identity of a retained loser** at any cell first written inside the image, because a summary tile carries no per-cell stamps. ADR-006 and DP-A8 promise those losers, and BOOTSTRAP lists conflict honesty as differentiator 3, so an approximation is not available | TD-45, TD-31 and TD-24's residual all stay open: cold open replays (7.86 s at 1M cells), three retained snapshots cost three copies (307 MB), state is not adoptable | **The encoding question is now MEASURED and settled (D-102, W-IMAGE-STAMPS).** Delta-varint stamps cost **3.1 B/cell**, projecting to 153.2 MB at 10M cells against A-001's 400 MB bar — a pass with 2.6× headroom. The naive layout this entry originally assumed *fails* at 428.8 MB. **ADR-036 is written and accepted**, and **the kernel half is now built and proven** (D-111): `WinnerStamps::from_log`, the delta-varint sidecar behind `IMAGE_VERSION` 2, `State::apply_tail`, and the refusal of a tail that predates the image. The load-bearing test passes — an adopted image plus a tail retains the *same losers* as a full replay, not merely the same hash. **CLOSED (session 21).** The container half is implemented under ADR-036 Amendment 1: `snapshots.body` is `[image][covered op ids]`, `verify` decodes instead of replaying, and coverage excludes ops the image cannot represent — so `prune_floor` can never contain one and **DP-A5 holds by construction**. Measured: **cold open 7.86 → 1.79 s, container 307 → 148 MB**. TD-45 and TD-31 are paid with it. A freshly-replayed state has none — that is ADR-005's whole point — so they are either kept resident (**17 B/cell, 123.6 → ~285 MB at 10M; passes A-001 but makes promotion vacuous and retires ADR-005's central claim**) or reconstructed at snapshot time from the log (no resident cost, ~320 MB temporary unless streamed per tile band). Recommended: the latter. Also still needed either way: record the image's greatest canonical key so an out-of-order tail is refused rather than misplaced |
| ~~TD-47~~ **PAID (session 20) — the assertion was false; the mechanism is real** | ~~"`tools/gates.ps1` requires PowerShell 7 and aborts on a Windows PowerShell 5.1 host"~~ → **measured false.** The script runs top to bottom, one invocation, **all gates green, exit 0** under Windows PowerShell 5.1.26100.8875 on this host, in all three ways it is ever invoked: bare, with every stream redirected to a file, and dot-sourced. It was **never run under 5.1 before being written down** — the same defect as TD-48, one row up, filed one session later | The row was reasoned from a true fact about 5.1 and never checked against the artefact it accused | 5.1's `NativeCommandError` **is real and was reproduced** (D-104): `cargo clippy` merged into the pipeline throws under `$ErrorActionPreference = 'Stop'` purely because cargo prints `Finished ...` to stderr. But 5.1 wraps stderr *only when it is merged into the pipeline*, and `gates.ps1` never merges it — it lets stderr reach the console and judges by `$LASTEXITCODE`. So the trap is **latent, one redirection operator away**, not active | **CLOSED session 20.** Nothing needed fixing to make it pass; what it needed was *running*. Two gates now stand where the assumption did: a runtime probe (a native command writing to stderr and exiting 0 must not derail the run) and a static grep refusing any stderr-into-pipeline redirection in `tools\*.ps1`. Both run first and cost milliseconds, so an incompatible shell is named in two seconds rather than three minutes into a cargo build |

| ~~TD-51~~ **PAID (session 20)** | ~~A direct argument and a range cell coerce differently, and the engine treats them alike.~~ → two rules where there was one (D-109). W-ORACLE 88.2% → 89.3%. Original entry: `SUM("7",1)` is 8 and `SUM(TRUE,1)` is 2, while the same values *inside a range* are skipped; `SUM("abc",1)` is `#VALUE!` where a text cell in a range is ignored. Same split in `COUNT`, `MAX`, `MIN`, `PRODUCT` | **~15 measured** — `SUM` 6, `COUNT` 3, `MAX` 2, `MIN` 2, `NA` 1, `AVERAGE`/`PRODUCT` remainder | Row 6 implemented one rule for both, because the documented description of `SUM` ("ignores text and logicals") describes only the *range* half | Every aggregation over a literal or a nested call diverged | **CLOSED session 20**, filed and paid in the same session. The fix was where the entry predicted — `Operand` already carried the distinction — but it came with a trap the entry did not foresee: `SUMIF`/`AVERAGEIF` wrapped their picked cells as individual `Operand::Value`s, which would have put *range* data through the new direct-argument coercion. They now build one `Operand::Range` |
| ~~TD-52~~ **PAID (session 21)** | ~~An omitted argument is a parse error rather than a blank.~~ → the argument list accepts an empty slot, and a formula's *result* is materialised (D-110). Original entry: `=IF(TRUE,,2)` is `0` in Excel and `#NAME?` here; likewise `=IFERROR(1/0,)`. Separately, a blank *result* renders as `0`, so `=IF(TRUE,A1,2)` with `A1` empty is `0`, not blank | ~5 | The parser has no concept of an empty argument slot: the comma-separated list expects an expression between commas | Any formula using Excel's shorthand for "this branch is empty" failed outright instead of returning zero | **CLOSED session 21.** Two rules, not one: an omitted slot parses as blank, *and* a blank reaching the top of a formula becomes `0`, because a blank is a property of a **cell** and a formula always occupies its cell. The second is applied at `eval_top` only — a blank *sub*-expression stays blank, or `ISBLANK` and `COUNTBLANK` would have nothing to see and `SUM` would add a zero where it must skip |
| ~~TD-53~~ **PAID (session 21)** | ~~`IF`'s condition coerces text logicals but not numeric text, and the engine does the opposite.~~ → `truthy` is now Excel's condition rule (D-110), shared by `IF`, `IFS`, `NOT`, `AND`, `OR` and `XOR`. Original entry: `IF("TRUE",1,2)` is 1 and `IF("true",1,2)` is 1, but `IF("1",1,2)` is `#VALUE!` | ~4 | Row 6 sent the condition through the ordinary text→number coercion, which accepts `"1"` and rejects `"TRUE"` — exactly inverted | An imported model with a text-valued flag column evaluated backwards or errored | **CLOSED session 21**, and it paid more than it was priced at: sharing the corrected rule fixed `NOT("TRUE")` and, once `XOR` was given `AND`/`OR`'s skip-and-require-one behaviour, two `XOR` cases as well. **`IF`, `IFERROR`, `IFNA`, `NA`, `AND`, `OR`, `NOT` and `XOR` are all at 100%** |
| ~~TD-54~~ **PAID (session 20)** | ~~Errors are not transparent to the `IS` predicates and to `COUNT`.~~ → the blanket error guard in `call` now exempts the functions whose job is to *look at* a value (D-109). Original entry: `ISNUMBER(1/0)` is `false` in Excel and `#DIV/0!` here; `COUNT(NA())` is `0` and `#N/A` here | ~4 | Error propagation is uniform in the evaluator, which is right almost everywhere — these functions are the documented exceptions and were not carved out | A predicate written specifically to *test* for an error propagated it instead | **CLOSED session 20** with TD-51, as the trigger said. **One case remains and is not this row's**: `ERROR.TYPE` is simply unimplemented (`#NAME?`), which belongs to the function catalogue |
| TD-55 | **`LOWER`/`UPPER` do not implement the locale-independent special cases.** `LOWER(UNICHAR(304))` (Turkish dotted capital I) must be one character and is two here; `LOWER(UNICHAR(931))` must give final sigma `ς` | 3 | Case mapping uses Rust's `to_lowercase`, which is correct Unicode and not what Excel does | Three cases, and only for text outside ASCII | Low priority and explicitly named as such — it is the smallest cluster in the register and touches nothing else |

| ~~TD-56~~ **PAID (session 21, same session it was filed)** | ~~The winner-stamp sidecar is written at 66 B/cell where D-102 priced 3.10~~ → laid out per tile and positionally; ~1.9 B/cell measured. Original entry: — a global `BTreeMap<(RowId, ColId), Stamp>` serialised with its full identity keys (`row OpId` 24 B + `col OpId` 24 B + actor `u128` 16 B + two varints), instead of the **per-tile, positional** layout D-102 specified and measured | **Measured, not estimated**: W-OPEN-1M cold open 7.86 → 7.96 s and container 307 → 318 MB, with 265.6 MB of snapshot bodies. The model closes: 3 images at 66 B/cell plus 2.7M covered ids at 24 B ≈ 288 MB vs 265.6 measured | Nothing was chosen. D-102 measured this exact layout at 32 B/cell for the stamp alone and **rejected it as failing A-001 by 7%**; implementing the sidecar as a flat identity-keyed map arrived back at it, worse, because the keys are stored per entry | **TD-45, TD-31 and TD-24's residual all stay open** — the container half is built and correct and buys none of them. This is the single defect between here and all three **CLOSED.** Laid out per tile and positionally, exactly as the trigger said. Worth recording: **not one meaning test needed touching** — the round-trip, loser-equivalence and refusal tests pin what the section means, and the rewrite only changed how it is spelled. That is the argument for writing them first, cashed |

| TD-57 | **The covered-op-id list is now the dominant snapshot cost**: 2.7M ids at 24 B each is **64.9 of the 95.6 MB** of bodies in W-OPEN-1M, where the three images together are 30.7 MB | Chosen at implementation time: a flat `(actor, counter)` list is exact, trivially verifiable, and needed no schema change — the body already had room. Correctness first, and the number was measured before optimising | The image half is now ~3× smaller than the bookkeeping beside it, so further work on the image buys little until this moves | A per-actor **run** encoding: every id in this corpus comes from one actor with a dense counter range, which collapses to a pair. Do it when container size matters again, or alongside the anti-entropy Merkle (TD-26) which wants a compact coverage summary anyway |

| TD-58 | **The view axis rebuilds its prefix sums in O(n) on any structural edit.** docs/31 §15.2 specifies an **order-statistic tree** with cumulative extents, maintained incrementally | Queries already meet the documented O(log n) — `index_at_pixel` is a binary search and it is what a scrollbar drag costs. It is *maintenance* that does not: inserting one row re-walks the axis | Measured at **31.9 ms to build a 1,000,000-row axis** (W-SCROLL). Paid on every structural edit, so a collaborator inserting rows into a huge sheet stutters where scrolling does not. Nothing at Q2's current scale notices | A structural edit becoming interactive-rate on a million-row sheet, or the same tree being wanted by A1 parsing and range enumeration — docs/31 calls it "the single most performance-critical index in the kernel", so it should be built once and shared, not three times |
| ~~TD-59~~ **PAID (session 22)** | ~~The grid draws no text.~~ → rustybuzz shaping, ab_glyph rasterisation, a bundled `DejaVu Sans`, and a glyph atlas that shares the fill pipeline (ADR-038). Cost **8 crates**, shell closure 231 → 239 of 280. Original entry: Cells are drawn by *kind* — has a value, holds a formula, is an error — which is real information from the kernel, but a spreadsheet whose cells you cannot read is not one | docs/31 specifies a glyph atlas over **rustybuzz**-shaped runs with bundled fonts for metric determinism. That is a font stack, a shaping decision, a bundled-asset licensing question and an atlas allocator — four decisions, none of which belong at the end of a renderer commit | The grid is not yet usable *as a grid*. Everything structural around it — virtual scroll, the instance pipeline, the scene, the theme — is done and independent of it **CLOSED session 22.** The ceiling did *not* need revisiting — the earmark survives with 41 crates left — because `ab_glyph` was already in the closure via `winit`. Text added **no pipeline, no pass and no draw call**: a glyph quad and a cell fill are the same instance, differing only in which part of the atlas they sample |
| ~~TD-60~~ **PAID (session 23)** | ~~The windowed path is a stub~~ → `ehkatra-shell` with no arguments **opens a real winit window and presents to the compositor** (ADR-039). Resize, per-monitor DPI, vsync, the wheel and the full keymap are live; `--present N` presents N scroll frames against the real compositor and exits with the numbers. **W-PRESENT, measured: frame cost p50 1.98 ms, p99 3.27 ms** against docs/31’s 8.3 ms, 1 of 300 frames over budget (index 299 — the teardown frame), on a 1600×1000 surface at 1.25×. Cold launch **39.8 ms** against docs/31’s 1.0 s. Original entry: | Offscreen first was deliberate and it stayed correct — `--render` and `--script` still produce deterministic PNG evidence on a machine with no display, and `gpu::Renderer::encode_scene` is now the single place a scene becomes GPU commands, so the presented frame and the PNG cannot differ | Was: no frame had ever reached a compositor, so the present cost was unknown. **What the measurement found is worth keeping**: timing the whole present is meaningless under `Fifo`, because `get_current_texture` blocks for vsync. The first run reported p99 10.2 ms and “over budget”, which was the 120 Hz refresh interval and not the renderer. The wait is now reported apart from the work, and only the work is judged against the budget | Paid |

| ~~TD-61~~ **PAID (session 23)** | ~~Formula cells render their fill but no text~~ → the scene reads `usk_calc::Engine::value`, not `State::cell`, so a cell shows its **computed** result; `usk_reduce::Session` owns the log, the state, the engine and the undo stacks, and `Session::view()` hands the renderer both halves at one generation (docs/27 §3). `demo/grid.png` shows column N summing its own row and a computed `#DIV/0!` beside an authored one. Original entry: | The shell reading `State` directly was right for values and insufficient for formulas | Was: a blank formula column. **A second defect the same fix exposed, found by looking at the frame rather than by a test:** `Engine::build` builds the graph and evaluates nothing, so a *freshly opened* workbook still rendered blanks. Every editing test passed, because the first formula a user types is a structural change that forces a full recalc and fills the sheet in behind it — the tests only ever looked after an edit. `Session::from_log` now recalculates on open; what that costs is TD-66 | Paid. The generation shimmer docs/25 asks for is **not** built and is folded into TD-66 |
| TD-62 | **Every visible cell is shaped every frame.** docs/31 specifies a *"numeric fast path (pre-shaped digit runs per style)"* that it estimates ≈ halves paint cost in dense sheets; there is no run cache at all | Correctness first, and the budget had room. The atlas already caches *glyphs*; what is missing is caching the shaped *run* for a given string and style | Measured: the CPU frame went **0.192 → 1.373 ms p50** when text landed, which is 17% of docs/31's 8.3 ms. Cost, not breach — but it is the single largest item in the frame now | A W-SCROLL frame approaching the budget, or the first style variations, which multiply the shaping work by the number of styles. The fix docs/31 names is the numeric fast path, and digits are most of a spreadsheet |
| TD-63 | **The bundled font's licence needs confirming before distribution.** The `dejavu` crate declares `MIT/Apache-2.0` and the supply-chain gate accepts it, but DejaVu upstream derives from the **Bitstream Vera** licence, which is permissive and is not literally either | Taking the font as a crate rather than a repo asset is the right call for versioning and for gate coverage (ADR-038); the crate's SPDX is the crate author's declaration, and it is the only thing `cargo-deny` can see | None today — nothing is distributed. The interest is that a gate reading `PASS` here is checking the wrapper's metadata, not the font's own terms, and that distinction is invisible unless written down | **Before the first installer.** An installer is where a licence question stops being theoretical; confirm the font's own terms and add the attribution its licence requires, or vendor a font whose SPDX is unambiguous |
| ~~TD-64~~ **PAID (session 24)** | ~~There is no clipboard~~ → `Ctrl+C`/`Ctrl+X`/`Ctrl+V` and the **fill handle** (ADR-040). A copy writes tab-separated *displayed values* to the OS clipboard and keeps the full block — formulas included — in process, so a same-application paste moves relative references and a paste into Notepad gets numbers. Cut defers its removal to the paste and does *not* translate, which is Excel’s rule. Fill extrapolates a numeric run, moves a formula, and repeats everything else. Original entry: | The dependency question was the real one and it was **measured**: `arboard` costs **28 crates** with default features and **7** without, because `image-data` drags in an entire image codec stack for a flavour a spreadsheet never writes. Shell closure 239 → 246 of 280. A hand-rolled clipboard would have cost zero crates and three platform backends of unsafe FFI, of which this host can test one | Was: the largest single gap in the editing surface. What it cost: 7 crates and two new latent items — **TD-69** (the fill series stops at Excel’s core three) and **TD-70** (a paste is one undo step per cell). The lexer ambiguity **TD-68** was found by a test written for this work | Paid |
| TD-65 | **The frame cost grows with the *document*, not the window**: 4.09 ms for a 50,000-row sheet against 1.56 ms for an 850-row one. By quad count the render loop does *not* walk the document — the frame is ~1,300 quads at any size, and `a_frame_costs_the_window_and_not_the_document` holds — so something else in the frame is O(rows), and it is **not attributed** | Measured rather than profiled, and a register entry that guessed at the cause would be worse than one that says it does not know. The suspects are `Axis::index_of`, a linear scan the frame makes several times, and `Viewport::visible`’s anchor resolution, which makes two more | Measured: W-KEYSTROKE paint p50 **1.557 ms at 850 rows → 4.086 ms at 50,000**. Not a breach — docs/31’s scroll budget is 8.3 ms and its keystroke budget names a 10k sheet — but the *shape* is wrong, and a wrong shape is what a breach is made of. Removing one duplicated resolve moved it 4.18 → 4.09 ms, which is the evidence that the obvious suspect is not the whole answer | A profiler on the frame path, which is the honest first step. Making `Axis::index_of` O(log n) costs memory proportional to the axis (~28 B/row sorted) and should not be paid before it is known to be the cause. Shares a fix with TD-58 if the answer is the axis |
| ~~TD-66~~ **LARGELY PAID (session 25)** | ~~Opening a workbook parses and evaluates every formula synchronously, and the graph build is superlinear~~ → **the quadratic is found and fixed**. `extent_of` accumulated read rectangles one at a time and linearly scanned everything accumulated so far looking for a merge; an unbroken column merged on its first comparison and stayed O(n), a column with **gaps** merged with nothing and became O(n²). One sort plus one sweep. **1,000,000 rows: 218 s → 5.36 s (41×)**; 100,000 rows: 2,509 ms → 269–434 ms. W-CHAIN-100K's graph build also went ~910 → ~310 ms, from dropping a per-member `Vec`. Original entry: | The experiment that found it was varying the *gap pattern* and nothing else: the dense corpus has 50% more formulas and builds six times faster, which said immediately that the formula count was not the cost | **What remains, and it is the (b) half that was always separate**: the graph build and the recalc are still *synchronous, in front of the window*. docs/25's *“cells in recalc get a generation shimmer”* is unbuilt and needs the PAL `Compute` seam TD-17 waits on. At 1M rows the recalc is now the largest item at 16.1 s, and its per-cell cost grows with the result map (4.9 → 23.5 µs/cell) — the shape **TD-23** already names, offered as consistent-with rather than proven, since no profiler was run | The shimmer, when the `Compute` seam exists. The shell's default document is 5,000 rows where the whole open is ~44 ms, so nothing ships over the line today |
| TD-67 | **The incremental fold refuses whenever a second actor’s write lands on an occupied, unpromoted cell**, and falls back to a full `State::replay` of the log. In a two-person session that is every keystroke touching a cell the other person has already written | Sound and deliberate. A summary tile keeps no per-cell stamp (ADR-005’s memory argument), so the value such a write displaces cannot be *identified*, and a loser that cannot be identified cannot be retained (ADR-006). `State::apply_tip` reports `TipError::MayContend` and the caller re-folds, which is always correct and sometimes slow. Writes to empty cells and to already-promoted cells take the fast path from any actor, so the refusal is narrower than “collaboration is slow” | Unmeasured under collaboration, which is itself the gap: the single-actor case went **25.3 → 2.7 ms** at 60,000 cells and the collaborative case was never benchmarked. The refusal rate is not instrumented either — `Session::settle` discards the error rather than counting it | Instrument the refusal rate first; it may be rare enough to leave. The real fix is the per-cell stamp a summary tile does not carry, which is ADR-005/TD-10 territory and must not be reopened for a performance reason alone |
| ~~TD-68~~ **PAID (session 25)** | ~~The lexer reads `LOG10` as a cell reference~~ → the *parser* now treats a `CellRef` followed by `(` as a call, which is the rule its `Ident` arm has always applied. The lexer is unchanged and its shape test is still a shape test; what changed is that the grammar question is answered in the grammar. Original entry: | Latent when filed and confirmed so by probe: no function in the v0.1 set is spelled with a trailing digit. Fixed anyway, because the first one added — `LOG10`, `ATAN2`, `SUMXMY2` are all ordinary Excel functions — would have failed in a way that looked like a bug in *that* function | Was: `=LOG10(100)` parsed as a reference plus trailing garbage and evaluated to `#NAME?` by accident rather than on purpose. Two of the three new tests were confirmed to fail with the fix removed; the third (`#NAME?` reaches the user) passes either way and says so in its own doc comment | Paid |
| TD-69 | **The fill series stops at Excel’s core three.** A formula moves, two or more numbers in a straight line extrapolate, everything else repeats. Excel also fills dates by month or weekday, custom lists, and text with a trailing number (`Item 1` → `Item 2`) | Each of the missing four needs a model this build does not have: a date type (**TD-40** reads dates as serials), a locale (**TD-49**), a user-editable list, and a text-with-suffix parser. Building a private version of any of them inside the shell would put the worse copy in the wrong layer | A user dragging a date or a `Q1`/`Q2` header gets a repeat where Excel counts. Visible and wrong-feeling, and the most likely first complaint about the feature | The date model, which unblocks the largest slice. The custom-list case should wait for a preferences surface to hold the lists |
| TD-70 | **A paste or a fill is one undo step per cell.** Ten cells pasted takes ten `Ctrl+Z` presses to reverse | The shell composes existing `Command`s (`SetValue`/`SetFormula`/`ClearCell`) rather than adding a `Command::Fill` or a transaction, which keeps `reduce_v1`’s frozen semantics untouched and makes undo work with no new machinery (ADR-040 decision 5). It is the *right* first version and the wrong final one | Measured only as behaviour, not as cost: `a_paste_is_one_undo_step_per_cell_and_undo_reaches_all_of_it` asserts it so the gap cannot be forgotten. A large fill also emits one op per cell, which is a real op-log cost nobody has priced | A paste big enough that the op count matters, or the first user complaint about undo. Two candidate shapes: a **labelled undo group** in the reducer (docs/11 already has per-actor labelled groups, so this may be reachable without new op types) or a `Command::Fill` that compiles to a range op. The first is additive; the second is a vocabulary change and needs an ADR |
| ~~TD-71~~ **PAID (session 28, for range reads — the reads that dominate)** | ~~`State::cell` costs 214–322 ns, and a full recalculation makes one call per cell every formula reads~~ → **a range read no longer pays it per cell.** `TileStore::read_rect` resolves the column run's slots once per range and each row's slot once per row, fetches each tile once per (row, column band) rather than once per cell, and continues presence ranks incrementally while the in-tile index ascends; `Grid::read_rect` (default body = the old per-cell loop, normative) hands the whole rectangle down from the evaluator, and `EngineGrid`'s override overlays computed results at one binary search per column instead of one per cell. The identity-based single-cell path is untouched and every other caller still uses it. Original entry: | The slot maps are what ADR-034 buys: a stable identity→slot interning that survives inserts and keeps content-addressing intact. Three tree lookups is the price of asking that question three times per cell rather than once per *range* | **Profiled before building** (experiment 4, session 28): the three lookups price at ~90–110 ns of a ~195–270 ns read on this host — the register's named cause confirmed as the largest single term, with `Presence::rank`'s popcount walk most of the remainder, which is why the rect read amortises ranks too. After: the same 1×12 reads cost **64–78 ns/read** against `State::cell`'s unchanged 185–227 (≈3×), still flat at 500k rows; the wide profile shape went **4.30 → 2.73–2.94 µs/formula** at 100k and **4.46–4.55 → 2.87** at 500k. W-CHAIN-100K unchanged at 45–57 ms — its reads are single-cell and mostly hit computed results, so it never paid this cost, exactly as the entry predicted | Paid, at the cost of one new tile-store entry point, one defaulted `Grid` method, and one override — op encoding untouched, replay hashes verified unmoved (`c79fa533…`/`b58d5505…`). **Single-cell reads still pay the three lookups**; if a single-cell-heavy workload ever approaches a budget, that is a new row, not a reopening of this one |
| TD-72 | **The ZIP writer emits STORED entries only — there is no DEFLATE compressor** (D-121). A written workbook is the size of its uncompressed XML: measured **2.06×** the deflated input over the corpus | Zero new dependencies and zero new codec surface; a compressor is complexity without a threat (nothing hostile flows through a writer), and STORED is fully spec-compliant — all 20 corpus round-trips open in real Excel | File size only, and only on write. Small workbooks are ~3 KB either way; the multiplier matters at Excel-scale exports, unmeasured because no such workload exists yet | A real file-size complaint, or the first export workload where docs/31 names a size budget. The inflater's tables are already in the crate; a fixed-Huffman-only compressor is the cheap middle step if the trigger fires |
| TD-73 | **The writer spells C0 control characters as numeric character references** (`&#x1;`), which our own reader round-trips and XML 1.0 strictly forbids even in reference form — Excel may refuse text containing them | Dropping a byte of the user's text silently is a worse failure than writing a file a stricter parser rejects; our reader accepts the reference, so the round-trip property holds | None measured: no corpus file and no engine path produces such text today — it can only arrive via a future paste/import path | The first import path that can carry control characters in text. The fix Excel itself uses is the `_xHHHH_` escape convention, which needs matching decode support in the reader |
| TD-74 | **`is_lossless()` on a `WriteReport` counts `parts_unaccounted` it cannot name.** A source container part that the read fidelity did not classify is dropped on write with only a count, not a name | The reader keeps no bytes for parts it does not classify, so the writer has nothing to name — fixing this properly means the reader listing every part name, which is a reader change taken deliberately rather than smuggled in here | A fidelity report that says "1 part dropped" without saying which. Zero occurrences across the current corpus (every part is classified); the row exists so the gap is chosen, not discovered | The first real-world workbook whose report shows `parts_unaccounted > 0`; then the reader's `Fidelity` gains a named `parts_unclassified` list |

## W-ORACLE divergence debt (ranked by measured case count, session 12)
Filed from the first conformance run (74.2%, MEASUREMENTS.md). Ranking is by cases, not by guesswork, which is the point of having a runner: every entry below names how much of the missing 25.8% it is worth.

| ID | Debt | Cases | Chosen because | Repayment trigger |
|---|---|---:|---|---|
| ~~TD-33~~ **PAID (session 20)** | ~~Date semantics diverge broadly; the **1904 date system is not implemented at all**~~ → both systems modelled explicitly (`DateSystem` on the evaluation context, D-105). **+126 cases: W-ORACLE 74.2% → 83.5%**, and the 1904 corpus **20.0% → 87.7%**. `DATE`, `DAY`, `MONTH`, `WEEKDAY` and `YEAR` are now **100% in both corpora** | ~98 (+104 of the 130 1904 cases) | Row 6 shipped date *core* against documented behaviour (D-043); the oracle then measured five distinct rules none of which follow from the others (docs/50 finding 6) | **CLOSED session 20**, exactly as the trigger predicted — it was the largest cluster and it moved the headline most. The 1904 half was one missing model rather than a hundred fixes: every serial was off by the same 1,462. **Residue is not date arithmetic**: `__compat_1900_leap` and `__compat_serial_boundary` are now held down by `TEXT()` (TD-36), `DATEVALUE` and `EOMONTH`, which are unimplemented — those belong to TD-36 and the catalogue, not here |
| TD-36 | `TEXT()` is unimplemented | ~28 | Number-format code grammar is a language of its own, and v0.1 scoped formats out (BOOTSTRAP "styles beyond number format") | The format layer. Until then every `TEXT` case is `#NAME?`, which is at least honest |
| ~~TD-34~~ **PAID (session 20)** | ~~The criteria sub-language diverges: comparison operators in strings, wildcards, blank-vs-`""` handling~~ → all three implemented (D-107). `COUNTIF`, `COUNTIFS`, `SUMIF`, `SUMIFS` and `AVERAGEIF` are at **100%** but for the three cases TD-50 now owns. W-ORACLE 85.7% → 87.0% | ~20 | Implemented from documented behaviour; the oracle shows Excel's actual coercion rules differ | **CLOSED session 20**, and it was cheap exactly as predicted — the wildcard matcher came free from TD-35. The rule worth remembering: **criteria coerce and lookups do not.** `COUNTIF(range, 7)` counts a cell holding the *text* `"7"`; `VLOOKUP(7, …)` does not find it. The two families cannot share an equality predicate, which is why one was wrong when they did |
| TD-50 | **`SUMIF`/`AVERAGEIF` do not extend a short sum range.** `SUMIF(A1:A5,">25",H1:H3)` must sum `H1:H5` — Excel reshapes the sum range to the criteria range's dimensions from its top-left, so `H1` alone means `H1:H5` | 3 (`SUMIF/0015`, `SUMIF/0016`, `AVERAGEIF/0004`) | Filed in session 20 rather than bodged. An `Operand::Range` carries *values*, not the reference they came from, so a 3-cell operand simply has no H4 or H5 in it. Extending it needs the **reference**, which is the same information TD-16's implicit intersection waits on | The dependency graph supplying references rather than materialised values — repay with TD-16, since one change serves both |
| ~~TD-14~~ **PAID (session 20)** | ~~Approximate-match lookup returns `#N/A`~~ → implemented as the **binary search Excel actually runs** (D-106). `VLOOKUP`, `HLOOKUP`, `XLOOKUP`, `MATCH`, `FIND`, `SEARCH` all reach **100%**; W-ORACLE 83.5% → 85.7% | ~24 | See the TD-14 row above — a deliberate v0.1 refusal, **now measured rather than assumed** | **CLOSED session 20**, exactly as the trigger required: the vectors existed first. The refusal was right to wait — over the unsorted key column `30,10,50,10` Excel answers the row holding **10**, which a linear "largest key ≤ needle" scan does not produce. That is not derivable from the documented contract, only from the implementation |
| ~~TD-32~~ **PAID (session 20)** | ~~`compat_parse_15` — literal truncation, `1E308` rejection, `1E-308` → 0~~ → implemented in the parser under a profile (D-108). `__compat_literal_parser` reaches **100%** (29/29); W-ORACLE 87.0% → 88.2% | ~14 | See the TD-32 row above (D-081) | **CLOSED session 20**, with the conformance runner watching exactly as the trigger asked. The rule that only measurement gives you: the truncation is on the **source text**, not the parsed double — `9999999999999999` has no exact `f64`, so by the time it is a number the digits Excel drops are already gone |
| ~~TD-35~~ **PAID (session 20)** | ~~`SEARCH`/`FIND` have no wildcard sub-language (`?`, `*`, `~` escape)~~ → implemented once, shared by `SEARCH` and every exact-match lookup (D-106). `SEARCH` and `FIND` are both at **100%** | ~6 | Row 6 implemented literal search only | **CLOSED session 20**, taken with TD-14 because the two share the matcher. The split it encodes is real: `SEARCH` has the sub-language and `FIND` has none — the same line as case-insensitivity, which is not a coincidence. The escape has to work on **both** paths: `a~*c` is a literal, and the literal still needs its tilde removed before comparison |
| TD-49 | **Date-shaped text is parsed as ISO-8601 only.** `YEAR("2024-03-15")` works; `YEAR("15/03/2024")` does not, and neither do the other locale-dependent spellings Excel accepts. `DATEVALUE` is still unimplemented (`#NAME?`) | ~2 measured, plus an unknown number outside the corpus | Filed in session 20 alongside TD-33 rather than guessed at. The `15/03/2024` fixture exists in the corpus, but **what it means depends on the capture host's regional settings** — `dd/mm` here, `mm/dd` on a US host. Implementing it from this corpus would silently encode one machine's locale as engine behaviour, which is the opposite of what an oracle is for | A locale model — an explicit workbook/host locale the parser is given, rather than inferred — or a second capture on a differently-configured host that shows which parts are locale-invariant. Until one exists, ISO is the only spelling that means the same thing everywhere |
| TD-75 | **Borders are not modelled.** ADR-041's facet set is number format, font, fill and alignment; `<borders>` in a written `styles.xml` is still the one-entry skeleton D-122 described, and a border on a cell we read is silently absent from the model rather than reported as a loss | Unknown in the corpus — the reader's 21 files carry no borders, so the interest is unmeasured, which is itself the honest statement | A border is a *facet*, not a new op type (ADR-041 decision 3 exists precisely so this is cheap): tag `0x05`, body of four or five edges × `(style u8, argb u32)`, plus the `<borders>` table in the writer. It becomes urgent at the first real-world import that has any, i.e. the first customer file. Also: the reader must start recording a **named loss** for a border it drops, which it does not today — that half is the part that is arguably already a defect rather than debt |
| TD-76 | **Theme-referenced colours are read as absent.** `<color theme="1" tint="-0.5"/>` resolves to no colour rather than to the theme's colour, because `xl/theme/*` is in `is_known_unmodelled` and is never read. A font that is "black by theme" reads as the default black, which is right by accident; a font that is theme-accent-2 reads as black, which is wrong | Every Excel-authored file uses theme colours for almost everything, so this is the single largest gap in the style read. Unmeasured against real files; the corpus uses explicit `rgb=` | Reading `xl/theme/theme1.xml`'s colour scheme and applying the `tint` transform. Triggered by the first fidelity run against Excel-authored files rather than hand-built ones — which is also what would put a number on TD-75 |
| TD-77 | **No named cell styles.** Excel's `cellStyles`/`cellStyleXfs` layer ("Heading 1", "Currency") is read past, not read: a cell's format is resolved from its own `xf` only, so a cell whose formatting comes *from* its named style reads as unformatted | Named styles are a second mutable object with their own concurrent-edit story (two actors editing "Heading 1" while a third applies it), which is a strictly larger CRDT problem than the one ADR-041 solved. Bought before anyone asked for it would have been the wrong trade | An ADR, because it is a new object and not a facet. The path ADR-041 leaves open: a `StyleRef` facet resolving through the same interner, with the named style itself an OR-map keyed by name. Trigger: a user-visible request, or an import fidelity run where named styles are a measurable share of the loss |
| TD-78 | **Style resolution is a linear scan over the rules for a facet**, newest-first, per cell. `O(rules)` worst case with no index | Measured, not assumed: **112–195 ns/cell/facet** at 64 rules (W-STYLE-COLUMN), flat in sheet size, ≈0.1 ms for a 40 × 20 viewport against docs/31's 8.3 ms frame budget. Cost, not breach — and the scan is the *chosen* half of ADR-041's memory trade, not an oversight | A W-SCROLL frame in which style resolution is measurable, or a workbook whose rule count reaches the low thousands (a spreadsheet has tens to hundreds of formatting *operations*, which is why this is not urgent). The fix is a band index over rule windows — the same shape as the calc graph's `BandIndex`, and TD-20's lesson applies: profile it before building it |
| ~~TD-79~~ **PAID (session 32)** | ~~There is no font fallback: the bundled font cannot draw what the IME delivers.~~ → a fallback chain under **D-125**, TD-79's option (b). A codepoint the bundled face lacks resolves through a *bundled preference order* to a system face, the atlas is keyed `(face, glyph, device_px)`, and the resolved face is recorded per run (`Run::faces`) so a layout divergence between two hosts is explainable rather than mysterious. **Cost: 3 crates** — `fontdb` with `default-features = false, features = ["fs"]`, chosen after measuring `font-kit` at 19 (W-DEPS-FALLBACK); shell closure 246 → **249/280**, leaving 31 for accesskit. The test that asserted the defect is deleted and replaced by one asserting `にほん` is three *different* glyphs; `demo/editing-ime.png` now shows a live kana composition where session 31 could only show Latin. Original entry: | Session 31 wired native composition end to end (D-124) — a Japanese composition reaches the editor, splices at the caret, underlines and commits correctly, all proven without a display — and then `DejaVu Sans` shapes every CJK codepoint to `.notdef`. The user types `にほん` and the grid draws **three identical boxes**. The IME plumbing is right and the pixels are wrong, which is a different defect with a different fix | **Measured, and it is total, not partial**: `text::tests::the_bundled_font_covers_latin_and_not_cjk_which_is_what_td79_is` shapes `にほん` and asserts all three glyphs sample the *same* atlas rect — three distinct characters rendering identically is `.notdef`, three times — with three Latin letters as the control that the check can fail. Advances and cluster offsets are correct, so the caret still lands in the right place; only the glyphs are wrong. Blast radius is every non-Latin script, not only CJK | A **font fallback chain**: enumerate system faces, pick per-run coverage, key the atlas by `(face, glyph, size)`. It is a real cost — a system-font crate (`fontdb`/`font-kit` class) against the **34 crates** of ADR-038 headroom that accesskit is already earmarked for, plus the docs/31 constraint that layout determinism comes from *bundled* metrics, which a system face by definition does not have. That tension is the design question and it is not answered here. **Trigger: the first non-Latin user, or the docs/48 item this session could not close** — *"IME validated by native JP/CN/KR typists"* is unreachable while the result is boxes, so TD-79 blocks that checkbox and should be paid before it is attempted. The Latin composition in `demo/editing-ime.png` is what the shell can honestly show today |
| TD-80 | **The first codepoint the bundled face cannot draw stalls the frame it appears in.** Building the system font database is synchronous and happens inside `TextEngine::layout`, so the first non-Latin character a session ever shows costs **238–412 ms** where docs/31 budgets 16 ms keystroke→paint. Every one after it costs 22–49 µs | Deliberate, and the alternative was worse: warming the database needs a background thread, a hand-off into `TextEngine`, and a decision about *where* it is started (at `App::open`, not at `TextEngine::new`, or 114 tests and every benchmark spawn a 300 ms file-scan they never use). That is a second layer, and stacking it unverified on the fallback layer this session just proved is exactly what DP-C4 forbids. Fallback with a named one-time stall is strictly better than boxes forever | **Profiled, not guessed** (`--fonts`, W-FALLBACK): the enumeration is **203–321 ms of it** — 379 faces on M1, each file read and parsed to learn its name — and the pick is the small remainder. So the fix is to move the enumeration, not to make it cleverer. Two residuals ride along: `pick`'s last-resort pass re-reads every face on a host whose fonts are in none of `PREFERRED`, which would be seconds rather than milliseconds and is untested because this host never reaches it; and the resolution is not shared between processes, so `--script` and the window each pay it once | Warm the database on a background thread started at `App::open`, so the enumeration overlaps the seconds between launch and the first non-Latin keystroke; a resolve that arrives first blocks exactly as it does today and never worse. Re-measure W-KEYSTROKE **and** cold launch (39.8 ms against docs/31's 1.0 s) — a background file scan competing with startup is the thing that could go wrong. Trigger: **before the docs/48 item TD-79 unblocked** — *"IME validated by native JP/CN/KR typists"* — because a native typist's very first keystroke is the one that pays this |
| TD-81 | **A run that leaves the bundled face is not metric-deterministic across hosts.** Its advances come from whichever system face the host resolved, so two machines with different fonts installed lay the same text out differently — the residual half of **D-125**, which narrows docs/31's *"layout determinism is metric-determinism"* to the codepoints the bundled face covers | Chosen, with the alternatives measured and on record: bundling a CJK face is ~16 MB and still wrong for Devanagari, Thai and Arabic; giving up the claim entirely (TD-79 option c) retires a docs/31 promise and would need an ADR. Drawing the character from a host face and *saying which one* keeps the promise everywhere it can be kept | Bounded and named rather than silent: `Run::faces` records the slots, `TextEngine::face_name` names them, `App::resolved_faces` reports the session's set and `--script` prints it, so a divergence is diagnosable in one line instead of being mysterious. What it costs: a shared workbook containing CJK can render at different widths on two collaborators' machines. **Nothing in the kernel is affected** — no op encoding, no state hash, no replay | Only a feature that needs cross-host *pixel* agreement — printing, PDF export, or a server-side render that must match the client. Then the answer is per-document embedded fonts (what XLSX and PDF both do), not a bigger bundle; the run record this decision already keeps is what such a feature would read to know which faces to embed |

Two things this table is deliberately *not*: it is not a plan (BOOTSTRAP's row order still governs), and it is not a list of bugs found by reading code. Every row is a measured count against a captured oracle, and every row's fix is gated by vectors that already exist.

Paid debt moves to an archive with the date and the diff link. The register is reviewed at every release train; unpriced debt found in review is filed against the team that took it silently.
