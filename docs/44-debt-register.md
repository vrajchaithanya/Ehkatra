# 44 — Technical Debt Register
Status: Living · Rule: debt is *chosen* and recorded at decision time with a repayment trigger, or it is a defect.

| ID | Debt | Chosen because | Interest we pay | Repayment trigger |
|---|---|---|---|---|
| TD-01 | DataFusion for Q1 SQL instead of tile-native engine | quarter scope | extra copy sheet→Arrow; ACL enforcement wrapped, not planned-in | Arrow layer lands (H2) or query p95 breach |
| TD-02 | ADRs 001–026 written post-hoc | rewrite speed | rationales may hide rejected-alternative context | live-ADR rule from now; backfill alternatives when touched |
| TD-03 | Spec budgets are targets, not measurements | design preceded rig | credibility risk; possible re-design | MEASUREMENTS.md Q1 W13 |
| TD-04 | XLSX write values-only in Q1 | scope | demo asymmetry (read ≫ write) | Q2 XLSX write + corpus |
| TD-05 | No gRPC/Arrow in H1 | desktop-first trim | bulk integrations wait; REST/NDJSON only | first data-platform design partner |
| TD-06 | Auth = bearer token in Q1 skeleton | scope | not production-safe; must not leak into Q3 | server plane alpha (Q3) replaces wholesale |
| TD-07 | Charts/pivots absent at GA-alpha boundary | scope discipline | pilot objections expected | H2 top of queue; pre-briefed to partners |
| TD-08 | Single relay process (no HA) through Q3 | ops simplicity | availability risk in pilots | GA operational-readiness gate |
| ~~TD-09~~ **PAID (session 9)** | ~~Promotion granularity is the whole 16,384-cell tile~~ → promotion is now per contested **cell**; amplification 16,384× → 1×, measured under W-TILE-10M. A-001 restored under collaboration (123.6 MB vs 400 MB). Original entry | ADR-005 as designed; the cheap summary is per tile, so the promoted unit is too | Measured: one contested cell costs ~1 MB of per-cell metadata; 0.1% scattered contention drives 8.4 → 74.5 B/cell and puts 10M cells at ~745 MB vs a 400 MB budget (A-002 FAILED, MEASUREMENTS.md) | **CLOSED session 9.** Chose per-cell stamps over sub-tile blocks: blocks only move the amplification constant, cells remove it. Row 10's gate is satisfied. |
| TD-10 | Promotion detects *multi-writer cells*, not true concurrency | v0.1 `Op` carries `{id, lamport, payload}` — docs/10's causal `deps` delta is not implemented yet, so causally-ordered cross-actor writes are indistinguishable from concurrent ones | Over-promotion: sequential hand-offs (A edits, then B edits the same cell later) pay full per-cell metadata for a conflict that never existed | `Op` gains `deps` (Row 10 sync). Re-run `tools/tile-bench`; expect the A-002 numbers to improve but **not** enough to close TD-09 on its own |
| TD-12 | `Decimal` is a 128-bit scaled integer, not IEEE 754-2008 decimal128 (ADR-035) | Full IEEE decimal128 is thousands of lines plus a conformance suite, in a `no_std` kernel with a 5-crate budget; the available crates offer *fewer* digits (~28) than either | No bit-level interchange with other decimal128 implementations; no ±Inf/NaN; no quantum. In exchange: 38 digits vs decimal128's 34, and exact integer arithmetic | XLSX round-trip or an external system requiring true decimal128 bit-encoding. Localised behind `Value::Decimal` + the canonical encoder |
| TD-13 | `compat_final_adjust`'s `1e-15` threshold is documentation-derived, not oracle-captured (D-041) | The Excel COM capture harness (ADR-024, A-007) does not exist yet, and docs/32 warns the documentation lies | A `compat` workbook may zero — or fail to zero — a cancellation result where real Excel does the opposite. Shape of the rule is certain; the boundary is not | First oracle corpus run. This vector should be in the first capture batch |
| TD-14 | Lookup is exact-match only; approximate match returns `#N/A` (D-044) | Excel's approximate match is correct only on a sorted key, and v0.1 cannot verify sortedness. A binary search over unsorted data returns a confidently wrong number rather than failing | Imported workbooks using `VLOOKUP(...,TRUE)` — the Excel *default* — get `#N/A` instead of a value. A real compat gap, not a cosmetic one | Row 8 identity intervals give ranges a sortedness contract; implement then, with oracle vectors |
| TD-15 | `^` and `SQRT` use in-crate series/Newton rather than libm (D-046) | `no_std` kernel (DP-A3) and DP-A2's bit-identical-everywhere requirement; platform math libraries disagree in the last bits and drift between versions | Fractional exponents are accurate to ~1e-12, not correctly rounded. Integer exponents are exact, so the common case is unaffected | Oracle corpus (ADR-024) shows a conformance miss attributable to rounding |
| TD-16 | Range-to-scalar collapse takes the top-left cell, not Excel's implicit intersection (`@`) | Implicit intersection needs the *calling cell's* row and column, which arrives with the dependency graph | A formula like `=A1:A10+1` in a scalar context gives Excel's answer only when the caller sits on the first row | Row 7, when the dep graph supplies the evaluating cell's position |
| TD-17 | Recalculation is single-threaded; levels are computed but not executed in parallel | rayon sits behind the PAL `Compute` trait (DP-A3), which is unbuilt | A-003's "on 8 cores" is unvalidated. Currently harmless — the single-threaded path already beats the budget — but a wide model has parallel width this leaves on the floor | **Only when a real workload breaches the 200 ms budget single-threaded** (owner directive 2026-08-07, D-054). Do not build PAL `Compute` + the rayon bench speculatively; until then A-003 stays “confirmed (single-threaded path)” |
| TD-18 | `Engine::build` regroups the whole sheet; there is no incremental regrouping | Row 7's claim is about graph size and recalc cost, not edit-time maintenance | Editing a formula requires a full rebuild (699 ms at 100k formulas), so formula edits are not yet incremental — only *value* edits are | **Row 9 exit criterion** (owner directive 2026-08-07, D-054): the regrouping trigger must be wired when edits become ops |
| TD-19 | Graph build is 699 ms for 100k formulas, dominated by parsing | No parse cache; every formula string is parsed on build | Extrapolates to ~7 s for 1M formulas. Not a budget breach today (docs/31's 1.5 s cold open covers skeleton+viewport, not a full parse) but it is on the path to one | Row 11 snapshots — persist parsed formulas rather than re-parsing on open |
| TD-20 | The range index is band-bucketed, not the R-tree docs/13 specifies | A band bucket gives the right access shape for a fraction of the code | Stabs scan every group whose reads cross the band; degrades when many groups share bands | Profiles show band scanning dominating, or Row 8 moves rectangles into identity space |
| TD-21 | Two addressing models coexist in `usk-calc`: `refs` speaks identities, `Sheet`/`Engine` still speak ordinals (D-053) | Converting the dependency graph to identity intervals is larger than Row 8's scope and would put an unproven rewrite under the A-003 benchmark | Two ways to name a cell in one crate. Harmless while `Sheet` is only the graph's substrate; a correctness hazard the moment both are load-bearing | **Row 9 exit criterion** (owner directive 2026-08-07, D-054): Row 9 is not done until the ordinal `Sheet`/`Rect` addressing path is deleted and only identity intervals remain |
| ~~TD-22~~ **PAID (session 10)** | ~~Formula-vs-value cell LWW depends on full-replay order, not stamps (D-056)~~ → the registry is a stamped LWW register seeded by the replay pre-pass (D-063); order-independence proven over all 120 permutations of a mixed history, replay hashes unchanged | Both `State` constructors replayed in canonical total order, so last-applied-wins was correct with zero storage | An incremental merge would have resolved formula-vs-value conflicts wrongly | **CLOSED session 10**, before the first incremental-apply path existed — exactly as the trigger required |
| ~~TD-24~~ **LARGELY PAID (session 10)** | ~~`Session::refresh` re-folds the whole log per batch~~ → the fold is now taken **when state is read**, not when the log grows (D-071). W-SYNC-RELAY at 50 replicas: **120 min → 6.8 min, 17.6×**, with every protocol figure bit-identical | A summary tile keeps no per-cell stamps to compare against (ADR-005's memory argument) and promotion must be decided before the first write lands; stamping every cell is the 480 MB regression TD-09 removed | *Residual*: `State` is still not incrementally appliable, so a read after N appends is O(N). Reads are ~50× rarer than appends under sync, which is where the 17.6× came from — but a read-heavy workload (a UI repainting per op) would still pay | **Residual trigger CORRECTED at Row 11** (D-076): the snapshot does *not* give the fold a checkpoint, because a v0.1 snapshot body is the compacted op set and `verify` replays it — W-OPEN-1M's 2.10 s cold open is that replay. The residual closes only when the body becomes a **materialised state image** (docs/16's tile image), which is real work rather than wiring. Still not the resident writer index, which costs memory on sparse workbooks |
| TD-25 | Unknown op tags are a decode error, not preserved-opaque (D-066) | The canonical encoding carries no per-op length, so an unknown payload cannot be skipped — the decoder cannot know where it ends | DP-A5 forward preservation ("unknown op types are preserved, causally ordered, hashed opaque, retransmitted") is unimplementable on the wire until the encoding is framed. Mitigated today by negotiation: a peer newer than us is rejected at HELLO rather than sent ops it would mangle | The CBOR wrapper the stack decisions already anticipate, or a `u32` length prefix on the payload. **Before the first wire-version bump** — i.e. before any N−2 window is real |
| TD-26 | Anti-entropy answers "what does the peer lack" from the never-drop queue and the relay's retained log, not from a Merkle comparison | Exact and O(unacked) for the two-replica and relay-fanout cases Row 10 proves; docs/15's tile-Merkle needs a snapshot Merkle that does not exist yet | Localising divergence inside a *large* shared history costs a retained-log scan instead of docs/15's O(log n) | Row 11 lands the snapshot Merkle; wire it here then |
| TD-30 | **A single snapshot is not a fallback** (D-075). docs/16 promises salvage from "the *last valid* snapshot", but the container keeps one and stores only the uncovered tail in `ops`, so a corrupt snapshot loses everything it compacted | Nobody stated a retention policy; the snapshot table's primary key is the watermark, which quietly permits exactly one row per watermark and no history | W-OPEN-1M measured it: corrupting the snapshot's final page lost 1,002,000 ops with **zero quarantined bytes** — nothing damaged, nothing left. The salvage path behaved exactly as specified; the policy above it did not exist | Pick one and state it in docs/16: keep N >= 2 snapshots, or retain compacted ops until a second snapshot verifies. Trades file size against recoverability, so it is docs/16's call, not the implementer's. **Before any real user data lands** |
| TD-29 | `snapshots.body` is stored uncompressed; docs/26 specifies "zstd tile+object image" | zstd is a fourth dependency stack on top of the nineteen crates `rusqlite` already cost (D-073), and compression changes no property Row 11 proves — the state hash is verified over the *decoded* ops either way | A 1M-cell snapshot body is 90 MB where zstd would plausibly make it a fraction of that; W-OPEN-1M's 108 MB container is mostly this | When container size becomes a user-visible complaint, or when the body becomes a tile image (TD-24's residual) and the two changes can be made together |
| TD-28 | **Blocker, not chosen debt.** Row 11's SQLite container adapter (ADR-031, docs/26 schema) cannot be built on this host: `rusqlite`'s only route is `bundled`, which compiles C, and the pinned toolchain ships a link-only gcc driver with no `cc1`; `libsqlite3-sys` 0.30 dropped the `winsqlite3` feature that would have linked Windows' own DLL (D-068) | Installing a C toolchain is a global install — forbidden by DP-S5 and CLAUDE.md's workspace boundary | Row 11 delivers its kernel half (snapshot format, docs/27 §2 machine, docs/16 SALVAGE) and stops at the storage seam. The container, WAL cadence, atomic-rename compaction and the kill −9 file test wait | A C toolchain on the build host, **or** CI (`ubuntu-latest`, where `cc` exists) becomes the first place the adapter runs — the same position the supply-chain job has held since session 3. Do **not** write the SQL blind in the meantime: DP-C4 forbids stacking an unverified layer |
| TD-27 | Transport is length-prefixed frames over loopback TCP, not the WebSocket docs/15 names (D-065) | ~20 crates against a DP-S2 ceiling of 40 that stands at 10, plus a second hashing stack DP-B9 forbids | A browser cannot reach the relay directly, so the web shell needs the swap before it can sync | The web shell (Q2), or the dependency budget being raised deliberately. Localised to `frame.rs` + `main.rs` |
| TD-23 | Calc results are an identity-keyed `BTreeMap`; full recalc regressed 53.0 → 92.6 ms on W-CHAIN-100K (+75%) | TD-21 required deleting ordinal addressing; identity keys are 48 bytes and go into a tree rather than a `Vec` slot | 100k tree inserts per full recalc. Both A-003 budgets still pass with margin (92.6 ms vs 200 ms; 0.328 ms vs 8 ms), so this is cost, not breach | Slot-index the results vector the way `usk-state`'s tile store already interns identities to dense slots — the machinery exists, it just is not shared yet. Do it when a W-* workload approaches its budget, or at Row 11 when snapshots need a dense value image anyway |
| TD-11 | `State::replay_sorted` trusts its caller to supply canonical order | The A-001 harness cannot exist otherwise: 10M cells is ~1.2 GB of ops for ~84 MB of state, so the log cannot be materialised | A caller feeding unsorted ops gets silently wrong LWW in summary tiles (a `debug_assert` catches it in dev builds only) | Row 11/12, when snapshot recovery and import become real callers — replace the precondition with a checked, streaming merge |

Paid debt moves to an archive with the date and the diff link. The register is reviewed at every release train; unpriced debt found in review is filed against the team that took it silently.
