# RFC-NNN — <change title>
Date: · Status: Draft | Accepted | Rejected · Touches: docs/<x>, docs/<y>

Use for changes spanning ≥2 module docs or touching a frozen irreversible. Solo context: this is a thinking tool — write it to argue with yourself before the code argues back.

## Problem
What's wrong or missing, with evidence (measurement, failed test, user report).

## Proposal
The design, at whatever depth the blast radius warrants.

## Impact
Docs to update · ops/model version implications (DP-A5) · migration needs (docs/26) · budget/bench implications (docs/31) · debt created or retired.

## Rollout
Order of changes, gates between them, rollback story.

## Open questions
Each with the default that wins if never resolved (autonomous sessions need defaults, not questions).
